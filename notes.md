# 记录（notes）

这个文档用于记录本仓库的实验想法、运行结果、TODO 和踩坑笔记。建议按日期持续追加。

## 仓库内容速览

- `CaSignal_Deconvolution.ipynb`：脉冲钙信号 + 指数核卷积 + 加噪；用最小二乘解析解做反卷积重建输入脉冲。
- `Generalization_error_simulation_experiment.ipynb`：线性回归在带噪观测下的多次训练仿真；用新样本估计平均泛化误差。

## 运行环境（可选填写）

- OS：Linux
- Python：
- 依赖：`numpy`, `matplotlib`

## 约定

- 记录尽量包含：参数、随机种子（如有）、关键输出数值/图、结论与下一步。
- 如果结果依赖随机性，优先固定 `np.random.seed(...)` 并记录 seed。

## 实验日志

### 2026-02-01

- **做了什么**：
- **参数/设置**：
- **结果**：
- **结论**：
- **下一步**：

## TODO

- [ ] 为两个 notebook 补充文字说明（背景/目标/方法/结论）
- [ ] 增加可重复性：固定随机种子、将关键参数集中到单独 cell
- [ ] 若数值不稳定：尝试用 `np.linalg.lstsq`/岭回归替代显式求逆

